include ./code.pug

//- Render a series of code examples related to an item.
//- @param {Object[]} examples - Code examples.
mixin examples(examples)
  if examples && examples.length
    section
      h6 Examples
      each example in examples
        section
          p!= spacedoc.markdown(example.description)
          +code(example.code, example.language)
