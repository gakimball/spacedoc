//- HTML for navigation sidebar.

nav.sd-nav
  each group in spacedoc.groupPages(site.pages)
    ul.menu.vertical
      if group.name !== '$default'
        li.menu-text= group.name
      each page in group.pages
        li.menu-item: a(href=page.fileName)= page.title
