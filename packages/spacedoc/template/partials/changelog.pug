mixin changelog(since)
  if since && since.length
    section
      h6 Changelog
      ul
        each snc in since
          li <strong>#{snc.version}</strong> !{helpers.markdown(snc.description, true)}
