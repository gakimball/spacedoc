//- Render the changelog of an item from its @since annotations.
//- @param {Object[]} since - Since annotations.
mixin changelog(since)
  if since && since.length
    section
      h6 Changelog
      ul
        each snc in since
          li <strong>#{snc.version}</strong> !{spacedoc.markdown(snc.description, true)}
