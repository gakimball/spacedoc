mixin properties(props)
  if props && props.length
    section
      h6= Properties
      table
        thead: tr
          td Name
          td Type
          td Default
          td Description
        tbody
          each prop in props
            tr
              td= prop.name
              td!= prop.types.join(', ')
              td
                if prop.default
                  code= prop.default
              td!= helpers.markdown(prop.description, true)
