//- Render the return value of a doclet.
//- @param {Object} ret - Return value.
mixin returns(ret)
  if ret
    section
      h6 Returns
      p <code>#{ret.types.join(', ')}</code> !{helpers.markdown(ret.description, true)}
