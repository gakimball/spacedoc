//- Render the return value of a doclet.
//- @param {Object} ret - Return value.
mixin returns(ret)
  if ret
    section
      h6 Returns
      if ret.types && ret.types.length
        p <code>#{ret.types.join(', ')}</code> !{spacedoc.markdown(ret.description, true)}
      else
        p!= spacedoc.markdown(ret.description, true)
