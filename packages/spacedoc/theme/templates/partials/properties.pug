//- Render the properties of an object, map, hash, etc. as a table.
//- @param {Object[]} props - Properties.
mixin properties(props)
  if props && props.length
    section
      h6= Properties
      table
        thead: tr
          td Name
          td Type
          td Default
          td Description
        tbody
          each prop in props
            tr
              td= prop.name
              td!= prop.types.join(', ')
              td
                if prop.default
                  code= prop.default
              td!= spacedoc.markdown(prop.description, true)
